/**
 * `bg-cribl-blue` / chart fills — must match `--color-cribl-blue` in `index.css`.
 */
export const CHART_CRIBL_BLUE = '#00CCCC'

/**
 * Edge fleet accent — must match `--color-cribl-edge` in `index.css`.
 * Used in the resource maps so connector lines, drop indicators, and
 * source-row accents that belong to an Edge fleet read as a lighter,
 * cooler sky-blue (vs. the strong cribl-primary teal that Stream uses).
 */
export const CHART_CRIBL_EDGE_BLUE = '#5fa8d3'
