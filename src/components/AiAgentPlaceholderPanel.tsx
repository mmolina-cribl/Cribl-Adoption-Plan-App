/**
 * @deprecated Use `AiAssistantPanel` from `./AiAssistantPanel`. Kept as an alias
 * so older imports keep resolving.
 */
export { AiAssistantPanel as AiAgentPlaceholderPanel } from './AiAssistantPanel'
